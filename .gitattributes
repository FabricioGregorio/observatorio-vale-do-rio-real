# Finais de linha normalizados em LF no repositório.
#
# Sem esta política, `core.autocrlf=true` no Windows materializa CRLF no working
# tree e `pnpm lint` reprova arquivos que ninguém editou.
* text=auto eol=lf
