node_modules/
.next/
*.tsbuildinfo
next-env.d.ts
.env*
!.env.example
playwright-report/
.lighthouseci/
test-results/
coverage/

# Área de trabalho local: capturas, medições, renderizações de conferência e
# derivados em preparo. Nunca versionar. Regra única — as três anteriores
# (h3-screenshots, h3-measure-*.bin, h4-screenshots) cobriam só parte do que
# foi parar aqui. Nada em /tmp/ é fonte: material com valor documental sai
# daqui antes de ser citado.
/tmp/

# Configuração local de harness de agente (perfis de preview, ajustes de
# sessão). É conveniência de máquina, não documentação do projeto.
/.claude/

# Artefato derivado de inventario-de-anexos.xlsx.
# Regenerado por scripts/derivar-inventario.py; nunca editar à mão.
inventario-de-anexos.csv
inventario-de-anexos.csv.origem

# Vínculo local com a Vercel; nunca versionar.
.vercel/

# Bytecode dos scripts Python; nunca versionar.
__pycache__/
